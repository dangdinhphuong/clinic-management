#ifndef STATISTICS_H
#define STATISTICS_H
#include "patient.h"
#include "medical_form.h"

// Các hàm thống kê cho Patient
void statisticsPatients(Patient* head);

// Các hàm thống kê cho MedicalForm
void statisticsMedicalForms(MedicalForm* head);

#endif // STATISTICS_H
